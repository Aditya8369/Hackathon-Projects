const express = require('express');
const router = express.Router();
const db = require('../db');

router.get('/', (req, res) => {
  const items = db.getChallenges();
  res.json(items);
});

module.exports = router;